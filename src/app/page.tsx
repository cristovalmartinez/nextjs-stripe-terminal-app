/**
 * Root page of the application. redirect is handled in nextjs config file to /payment route
 *
 * @page
 * @returns {JSX.Element} The rendered React components.
 */
const page = (): undefined => {
  return
}

export default page
